import { primitiveSize } from "@/styles/primitive/size-tokens.stylex";
import * as stylex from "@stylexjs/stylex";

export const buttonTokens = stylex.defineConsts({
  borderRadius: primitiveSize.size4,
  paddingBlock: primitiveSize.size4,
});
